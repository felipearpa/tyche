## 1. Backend

- [x] 1.1 Run the existing account username-update and propagation tests to verify the redesign requires no backend contract or persistence changes

## 2. iOS

- [x] 2.1 Expose `GamblerScoreItem` and the minimum `PoolGamblerScoreModel` construction API from the Pool package without exposing its layout internals
- [x] 2.2 Update the pure username-preview model factory to use `position = 1` and `beforePosition = 2`, alongside the signed-in account id, trimmed draft, current-user state, and fixed illustrative score
- [x] 2.3 Unit-test the preview factory for the exact position and before-position values, normal, whitespace-only, 100-grapheme, and long-truncating drafts, including that it performs no repository work
- [x] 2.4 Pass the signed-in account id into the pushed username route and rebuild `UsernameEditorScreen` with the intro, production `GamblerScoreItem`, counter, helper/error copy, and an in-flow, non-sticky "Save username" action immediately below the field guidance
- [x] 2.5 Migrate `UsernameEditorViewModel.saveState` from ViewingState `LoadState<String>` to `SaveState<String>`, mapping idle, saving, saved, and failure transitions without adding a custom async state
- [x] 2.6 Align initial text input behavior by populating the iOS field before one-shot focus, placing a collapsed caret after the final character (or at `0` when empty), preserving later user selection across SwiftUI and `SaveState` updates, and retaining unchanged/empty eligibility, in-flight disabling, inline failure, retry, and successful return behavior
- [x] 2.7 Add English, Spanish, and Spanish (Spain) localizations for the preview, field guidance, validation, save, retry, and failure states
- [x] 2.8 Add SwiftUI/view-model tests for one-shot initial focus, collapsed caret after prefilled and empty values, user-selection preservation across updates, live preview updates, grapheme count and clamp, action eligibility, every `SaveState<String>` transition, preserved failed value, retry, saving accessibility state, and Profile refresh after success
- [x] 2.9 Verify the screen with VoiceOver, Dynamic Type, the software keyboard, small viewports, light mode, and dark mode, including initial focus and end-caret placement, preservation of a manually moved caret, visible rank `1` and upward movement `1`, save-action reachability, and no forced per-keystroke announcements _(simulator-verified: light/dark, Dynamic Type AX-L and AX-XXXL, initial focus + end caret, mid-text caret preservation across appearance/type-size changes, rank 1 ↑1 visible with username truncating first, live preview/counter/eligibility, save action scrolls into reach, and two production save round-trips with Profile refresh; VoiceOver labels are test-asserted and the code posts no live announcements — the listening pass and SE device walkthrough were waived by sign-off)_
- [x] 2.10 Update the rendered-editor regression test to prove the preview receives `position = 1` and `beforePosition = 2`, visibly retains rank `1` and upward movement `1` for empty, long, saving, and failure states, truncates the username before displacing the rank rail, and exposes both localized values through accessibility

## 3. Android

- [x] 3.1 Update the pure username-preview model factory to use `position = 1` and `beforePosition = 2`, alongside the signed-in account id, trimmed draft, current-user state, and fixed illustrative score
- [x] 3.2 Unit-test the preview factory for the exact position and before-position values, normal, whitespace-only, 100-grapheme, and long-truncating drafts, including that it performs no repository work
- [x] 3.3 Pass the signed-in account id into the pushed username route and rebuild `UsernameEditor` with the intro, existing Pool `GamblerScoreItem`, counter, helper/error copy, and an in-flow, non-sticky "Save username" action immediately below the field guidance
- [x] 3.4 Retain `UsernameEditorViewModel` on the shared `SaveState<String>` lifecycle and use its carried saving/failure value and helpers instead of introducing a custom async state
- [x] 3.5 Align initial text input behavior by populating the Android field before one-shot focus, placing a collapsed caret after the final character (or at `0` when empty), preserving later user selection across Compose recomposition and `SaveState` updates, and retaining unchanged/empty eligibility, in-flight disabling, inline failure, retry, and successful return behavior
- [x] 3.6 Add English, Spanish, and Spanish (Spain) resources for the preview, field guidance, validation, save, retry, and failure states
- [x] 3.7 Add Compose/view-model tests for one-shot initial focus, collapsed caret after prefilled and empty values, user-selection preservation across recomposition, production-row reuse, live preview updates, grapheme count and clamp, action eligibility, every `SaveState<String>` transition, preserved failed value, retry, saving semantics, and Profile refresh after success
- [x] 3.8 Verify the screen with TalkBack, font scaling, the software keyboard, small viewports, light mode, and dark mode, including initial focus and end-caret placement, preservation of a manually moved caret, visible rank `1` and upward movement `1`, save-action reachability, and no forced per-keystroke announcements _(emulator-verified: light/dark, font scale 1.5, software keyboard open with save action unobscured, initial focus + end caret, live preview/counter/eligibility, rank 1 ↑1 visible; found and fixed a draft-loss on configuration change via `rememberSaveable` + `TextFieldValue.Saver`, re-verified surviving a dark-mode recreation; TalkBack content description is asserted by the instrumented tests — the listening pass and small-viewport walkthrough were waived by sign-off)_
- [x] 3.9 Update the rendered-editor regression test to prove the preview receives `position = 1` and `beforePosition = 2`, visibly retains rank `1` and upward movement `1` for empty, long, saving, and failure states, truncates the username before displacing the rank rail, and exposes both localized values through accessibility _(executed on the Pixel emulator: 3/3 pass)_
