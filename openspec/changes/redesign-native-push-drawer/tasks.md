## 1. Backend

No backend, persistence, or API work is required.

## 2. iOS

- [x] 2.1 Refactor `iOS/UI/Sources/UI/DrawerView.swift` around an interruptible SwiftUI reveal state while preserving the existing binding and style entry points; verify programmatic open, close, and mid-animation reversal settle at the requested endpoint without resetting to the opposite endpoint.
- [x] 2.2 Apply the content-group scale/fade and foreground translation, scrim, and subtle edge treatment through native SwiftUI modifiers, keeping the background opaque and layout stable; verify closed, half-open, and open previews plus an opening/closing recording show synchronized effects with no text reflow or premature content removal.
- [x] 2.3 Add continuous `DragGesture` handling with gesture takeover, release settling, cancellation, and logical-direction support; verify grabbing an in-flight animation, cancelled drags, vertical scrolling, and navigation-stack pop gestures behave as specified without UIKit gesture bridges.
- [x] 2.4 Isolate foreground interaction from the dismissal surface and integrate both routers' existing actions; verify tapping the pushed screen closes the drawer without activating underlying controls, destination callbacks run once, and returning from a destination leaves the drawer closed.
- [x] 2.5 Implement SwiftUI accessibility visibility/focus, escape dismissal, and Reduce Motion behavior; verify VoiceOver cannot traverse hidden content, focus returns appropriately, and reduced-motion opening/closing completes without decorative scale or spatial animation.
- [x] 2.6 Update `DrawerStyle.swift`, both drawer content views, `AccountHeaderDrawer.swift`, and `DrawerButtonRow.swift` to the planned hierarchy using SwiftUI colors and controls, bounded window width, safe areas, and scrollable large-text content; verify light/dark, compact/wide, short-window, right-to-left, and largest supported text-size previews keep every action reachable and remove affected direct UIKit color bridges.
- [x] 2.7 Restyle the existing production pool-summary component for both real and placeholder models, retain shared shimmer and account/avatar sources, and reuse icons or derive any changed assets from committed canonical vectors; verify loading/error states, cached reopening, owner/non-owner actions, deletion confirmation/pending behavior, and icon provenance satisfy the specification.
- [x] 2.8 Add and run focused behavioral coverage in the available iOS UI/app test targets for reversal/cancellation, hidden accessibility content, background tap isolation, and single destination dispatch; verify the UI package and app build at their supported deployment targets and no drawer-specific third-party or UIKit wrapper dependency was added.
- [x] 2.9 Validate both drawer hosts on an iOS simulator/device and record results and motion captures in this change's `verification.md`; verify normal/reduced motion, fast reversals, resizing, VoiceOver, long content, sign-out, and existing invite/delete flows, and note any remaining frame-pacing or accessibility issues before marking complete.
- [x] 2.10 Rework the iOS drawer drags to the revised gesture rules, superseding the drag regions implemented in 2.3: swipe open from anywhere on either host while its navigation path is empty, drag closed from anywhere on the open drawer (including action rows) or the foreground strip, cancel the touched drawer or host control's activation once a drawer drag is recognized, and detach drawer drags from the moment a destination is opened by passing each router's empty-path state through an additive `.drawer` parameter; verify with focused tests and real drags on a simulator (XCUITest or manual, since ViewInspector taps do not exercise gesture arbitration) that taps on drawer and host rows still activate once, drags starting on them move the drawer without activating them, vertical scrolling and the pool home tab bar keep working, and destinations keep the native back button and back-swipe without opening the drawer; record the results in `verification.md`, marking its earlier gesture-region notes as superseded.

## 3. Android

- [x] 3.1 Refactor `Android/ui/src/main/java/com/felipearpa/tyche/ui/PushDrawer.kt` around a single interruptible Compose reveal state while preserving caller ownership of the open target; verify programmatic open, close, and reversal continue from the displayed progress and settle correctly.
- [x] 3.2 Apply drawer-content group scale/alpha and foreground translation, scrim, and edge treatment using Compose layout and graphics modifiers; verify controlled animation frames and an opening/closing recording show a stable opaque background, stable text layout, synchronized dimming, and a completed exit before hiding content.
- [x] 3.3 Add continuous Compose drag input with velocity-aware settling, cancellation, interruption, density-independent thresholds, and logical-direction support, opening from a horizontal swipe anywhere on either host screen and closing from a drag anywhere on the open drawer (including action rows) or the foreground strip, with a recognized drag cancelling the touched drawer or host control's click; verify partial/fast drags and cancelled gestures reach valid endpoints, taps on drawer and host rows still activate once while drags starting on them do not, and vertical scrolling and the pool home tab row are not stolen.
- [x] 3.4 Implement foreground input isolation, a distinct dismissal surface, and Android Back precedence at the appropriate composable/host boundary; verify taps cannot reach underlying controls during closing, Back dismisses before popping the route, and predictive Back cancellation remains coherent if integrated.
- [x] 3.5 Implement drawer semantics, focus transitions, accessible dismissal, and system animation-duration handling; verify TalkBack cannot reach hidden drawer or background actions, focus restores after dismissal, and zero animation duration reaches usable endpoints immediately without lingering input interception.
- [x] 3.6 Update both Android drawer views, `AccounHeaderDrawer.kt`, and `DrawerButtonRow.kt` with Material surfaces, aligned native action rows, the separated footer, bounded width, system insets, and scrollable content; verify light/dark, compact/wide, short-window, right-to-left, and large-font previews keep all actions reachable with appropriate touch targets.
- [x] 3.7 Restyle the production pool-summary component for real and placeholder models, preserving shared shimmer, account/avatar sources, and existing action callbacks; verify cached reopening, loading/error states, single navigation dispatch, owner/non-owner visibility, deletion confirmation/pending state, and that any changed icon assets derive from the same canonical vectors as iOS.
- [x] 3.8 Add and run focused Compose UI coverage for swipe opening, drag-to-close from an action row without activating it, a synthetic horizontal child control keeping its drag, interruption, cancellation, background hit isolation, Back precedence, accessibility visibility, and disabled animations; verify relevant shared UI and app compilation/tests pass with the resolved dependencies and no Android View/XML drawer or third-party animation dependency was added.
- [x] 3.9 Validate both drawer hosts on an Android emulator/device and append results and motion captures to this change's `verification.md`; verify frame pacing, fast reversal, resizing, TalkBack, large text, sign-out, invite/delete flows, swipe opening from anywhere and drag closing from drawer rows, and gesture/button navigation (under gesture navigation, edge swipes follow system Back and dismiss an open drawer first; under three-button navigation, edge swipes open the drawer), comparing behavior with iOS while allowing native visual and motion differences.
