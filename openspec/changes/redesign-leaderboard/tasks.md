## 1. Backend

- [x] 1.1 Verify the existing pool-gambler-score response and `avatars/<accountId>.jpg` read contract provide every field required by the spec; keep HTTP routes, persistence, S3 policy, and deployment templates unchanged

## 2. iOS

- [x] 2.1 Add `CurrentUserColor`, `OnCurrentUserColor`, `CurrentUserContainer`, and `OnCurrentUserContainer` light/dark color sets to the shared UI asset catalog using design.md Decision 3 values, expose the generated resources consistently, and add contrast assertions for every foreground/background pair
- [x] 2.2 Extract the initial, deterministic color, contrast, and missing-initial icon rendering from `EmailAvatar` into a shared `InitialAvatar`; keep `EmailAvatar` as a source-compatible wrapper, generalize the existing `AccountAvatar` fallback input with backward-compatible defaults, and reuse `AccountAvatar` in leaderboard rows with gambler id plus username-backed fallback; support the `currentUser`/`onCurrentUser` override and add focused Swift Testing coverage for URL selection/revalidation, username initial derivation, missing-initial icon, generated contrast, signed-in styling, and unchanged email-avatar behavior
- [x] 2.3 Extend the existing `PostionIndicator` and shared `TrendIndicator` with backward-compatible size/style inputs only where needed, then compose those components in `GamblerScoreItem` per the list-row treatment in `assets/leaderboard-phone.png`: 44pt rounded rank tile with movement beneath it, 40pt circular avatar, one-line username, bold tabular score, stable missing-data dashes, and full-column separator; render the signed-in gambler as a full `currentUserContainer` band with stronger nested rank tile and current-user rank, username, "You", and score; do not add leaderboard-specific indicator types or duplicate trend logic
- [x] 2.4 Keep `GamblerScoreList` on the existing `RefreshableLazyPagingVStack`, integrating the redesigned rows without changing pager order, initial loading, refresh, append loading, append error, or retry behavior
- [x] 2.5 Preserve pull-to-refresh, append skeleton/error/retry behavior, and other-gambler navigation; replace gesture-only row activation with a full-row semantic `Button`, add a reusable shared row `ButtonStyle` driven by `configuration.isPressed`, verify immediate pressed/highlight feedback and cancelled-press behavior, combine each row into the specified VoiceOver announcement, and give the signed-in gambler row neither pressed feedback nor button semantics; leave `PoolHomeView`, its title, subtitle/header content, toolbar/navigation bar, and tab navigation unchanged
- [x] 2.6 Add/update localized strings for "You", points, movement, missing values, errors, and retry; verify English and Spanish plus light/dark and accessibility text previews
- [x] 2.7 Extend the shared position/trend indicator previews and tests to cover the leaderboard configuration and unchanged defaults for existing consumers; add Swift Testing/ViewInspector coverage for shared-component composition, up/down/steady/unknown movement, full-row current-user treatment including score and uploaded-photo behavior, missing fields, long usernames, pager order, and append continuity
- [x] 2.8 Build the Tyche scheme and perform phone visual checks against `assets/leaderboard-phone.png`, including both themes, full-row current-user highlight, neutral-row pressed/highlight feedback, avatar success/fallback, pull-to-refresh, append retry, completed and cancelled row presses, row navigation, VoiceOver, and dynamic type; add a regression check that the existing title, subtitle/header content, top navigation bar, and bottom tab navigation are visually and behaviorally unchanged
- [x] 2.9 Remove the explicit canvas background from `GamblerScoreList` (delete the `.background(Color(sharedResource: .surface))` override) and make neutral rows and placeholders transparent in `GamblerScoreItem` (neutral `rowBackground` → `Color.clear`) so the list and its rows inherit the base background from their container exactly like the Bets/History lists (SwiftUI system background: pure black in dark, white in light); keep the signed-in gambler's `currentUserContainer` highlight and the `surfaceVariant` rank tile; verify across all paging states that the leaderboard matches the Bets and History tabs in both appearances and that safe areas, `GamblerScoreListView`, `PoolHomeView`, title/header, toolbar, and navigation backgrounds are unchanged
- [ ] 2.10 Refactor initial and append-loading placeholders to construct the production `GamblerScoreItem` with `poolGamblerScorePlaceholderModel()` and apply the shared `.shimmer()` treatment; remove the separately maintained placeholder row anatomy while retaining only focused placeholder behavior that suppresses avatar loading, row actions, press feedback, and VoiceOver exposure; add coverage proving initial and append loading share this path and preserve production-row geometry and inherited backgrounds

## 3. Android

- [x] 3.1 Extend `ExtendedColorScheme` with `currentUser`, `onCurrentUser`, `currentUserContainer`, and `onCurrentUserContainer`, populate light/dark values from design.md Decision 3, and add contrast assertions for every foreground/background pair
- [x] 3.2 Extract the initial, deterministic color, contrast, and missing-initial icon rendering from `EmailAvatar` into a shared `InitialAvatar`; keep `EmailAvatar` as a source-compatible wrapper, generalize the existing `AccountAvatar` fallback input with backward-compatible defaults, and reuse `AccountAvatar` in leaderboard rows with gambler id plus username-backed fallback; support the `currentUser`/`onCurrentUser` override and add JUnit 5/Kotest coverage for request keys/revalidation, username initial derivation, missing-initial icon, generated contrast, signed-in styling, and unchanged email-avatar behavior
- [x] 3.3 Extend the existing `PositionIndicator` and shared `TrendIndicator` with backward-compatible size/style inputs only where needed, then compose those components in `GamblerScoreItem` per the list-row treatment in `assets/leaderboard-phone.png`: 44dp rounded rank tile with movement beneath it, 40dp circular avatar, one-line username, bold tabular score, stable missing-data dashes, and full-column divider; render the signed-in gambler as a full `currentUserContainer` band with stronger nested rank tile and current-user rank, username, "You", and score; do not add leaderboard-specific indicator composables or duplicate trend logic
- [x] 3.4 Keep `GamblerScoreList` on the existing `RefreshableLazyPagingColumn`, integrating the redesigned rows without changing pager order, initial loading, refresh, append loading, append error, or retry behavior
- [x] 3.5 Preserve pull-to-refresh, append skeleton/error/retry behavior, and other-gambler navigation; retain a full-row `Modifier.clickable` target and its theme-aware Material ripple/indication, verify immediate press feedback and cancelled-press behavior, merge each row into the specified TalkBack semantics, and give the signed-in gambler row neither ripple nor click semantics; do not introduce a leaderboard-specific interaction component, and leave `PoolHomeView`, its title, subtitle/header content, top app bar, and bottom tab navigation unchanged
- [x] 3.6 Add/update string resources for "You", points, movement, missing values, errors, and retry; verify English and Spanish plus light/dark phone previews
- [x] 3.7 Extend the shared position/trend indicator previews and tests to cover the leaderboard configuration and unchanged defaults for existing consumers; add JUnit 5/Kotest and Compose UI coverage for shared-component composition, up/down/steady/unknown movement, full-row current-user treatment including score and uploaded-photo behavior, missing fields, long usernames, pager order, and append continuity
- [x] 3.8 Run the relevant module tests and `:app:assembleDebug`, then perform phone visual checks against `assets/leaderboard-phone.png`, including both themes, full-row current-user highlight, neutral-row Material ripple, avatar success/fallback, pull-to-refresh, append retry, completed and cancelled row presses, row navigation, TalkBack, and font scaling; add a regression check that the existing title, subtitle/header content, top navigation bar, and bottom tab navigation are visually and behaviorally unchanged
- [x] 3.9 Remove the explicit canvas background from `GamblerScoreList` (delete the `.background(MaterialTheme.colorScheme.surface)` modifier) and make neutral rows and placeholders transparent in `GamblerScoreItem` (neutral `rowBackground` → `Color.Transparent`, drop the placeholder `.background(...)`) so the list and its rows inherit the base background from their container like the Bets/History lists (the app's dark `#121212`); keep the signed-in gambler's `currentUserContainer` highlight and the `surfaceVariant` rank tile; verify across all paging states that the leaderboard matches the Bets and History tabs in light and dark and that `PoolHomeView`, its title/header, top app bar, and bottom navigation backgrounds are unchanged
- [ ] 3.10 Refactor initial and append-loading placeholders to construct the production `GamblerScoreItem` with `poolGamblerScorePlaceholderModel()` and pass `Modifier.shimmer()` through a focused placeholder-styling input; remove the separately maintained placeholder row anatomy while retaining only focused placeholder behavior that suppresses avatar loading, row actions, ripple, and TalkBack exposure; add coverage proving initial and append loading share this path and preserve production-row geometry and inherited backgrounds
